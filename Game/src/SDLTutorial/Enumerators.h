#pragma once
//Enumerador per indicar l'estat del joc.
typedef enum GameState { MENU, GAME, QUIT, RANKING, GAMEOVER, DIFFICULTY,PAUSE};

//Enumerador per indicar la dificultat del joc.
typedef enum Difficulty { EASE, MEDIUM, HARD };